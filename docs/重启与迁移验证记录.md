# 重启与迁移验证记录

## 验证时间
- 2026-03-01（Asia/Shanghai）

## 迁移验证（Migration proof）
1. 创建数据库（干净库名）：`used_appliance`
2. 启动后端时由 Flyway 自动执行 `V1...V9` 迁移
3. 迁移脚本目录：`backend/src/main/resources/db/migration`
4. 幂等 seed 脚本：`backend/src/main/resources/db/seed/seed_demo.sql`

## 重启验证（Restart proof）
本次执行步骤：
1. 执行 `mvn -q spring-boot:run`
2. 观察应用启动日志
3. 预期检查 `/api/health`

本次实际结果：
1. 启动成功，Tomcat 监听 `8080`，应用日志出现 `Started UsedApplianceApplication`。
2. Flyway 在空库执行 `V1...V9` 全量迁移成功。
3. 已验证此前 `mockLogisticsGateway` Bean 重名冲突问题完成修复。

## 当前结论
1. 迁移脚本与 seed 资产已齐备，满足“可复现初始化”要求。
2. 重启验证通过，可用于答辩现场冷启动演示。
